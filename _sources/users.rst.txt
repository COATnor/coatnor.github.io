.. _users:

User types
==================================

COAT researchers and data managers can login to the Data Portal using their FEIDE account.
There are different roles which can be assigned to users

* regular users
* members of a module
* editors of a module
* administrators

To be registered in the Data Portal Users need to login once, then they will be available in the system.
After being registered, users can be added to modules (by administrators or module editors),
or selected in metadata from the users controlled vocabulary


Login with Feide
-----------------------------------------

To create a user in the Data Portal you need to login fo the first time using FEIDE.

Regular Users
-------------------------------------------

Regular users are users which logged in to the system at least once. A regualr user has no authorizations in the Data Portal,
and can visualize the same things that a not-logged in user can:

* See only public datasets
* Can't see private datasets
* Can't create new datasets

Members of a Module
-------------------------------------------

A user can be member of multiple Modules
Members of a Module:

* Can't create new datasets
* Can visualize private datasets of a Module

Editors of a Module
-------------------------------------------

A user can be editor of multiple Modules
Editors of a Module:

* Can add members to a Module
* Can create datasets for a specific Module
* Can visualize private datasets of a Module

Administrators
-------------------------------------------

They have superpowers